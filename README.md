# CoffEmail

A simple email client.

## Installation

To build the project, you need to have Java and Maven installed.
Then, you can run the following command:

```bash
mvn install
```

## Usage

To run the application, you can execute the following command:

```bash
mvn exec:java -Dexec.mainClass="com.coffemail.views.RegisterView"
```
